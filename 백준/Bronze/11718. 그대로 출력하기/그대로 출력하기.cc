#include <bits/stdc++.h>

using namespace std;
string words;

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    while (getline(cin, words))
    {
        cout << words << '\n';
    }

    return 0;
}